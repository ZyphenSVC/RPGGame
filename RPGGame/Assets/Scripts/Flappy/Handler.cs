using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Handler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start() {
        // GameObject gameObj = new GameObject("Pipe", typeof(SpriteRenderer));
        // gameObj.GetComponent<SpriteRenderer>().sprite = Assets.GetInstance().pipeHead;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
